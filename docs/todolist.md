# IM Code Agent Todo List

## P0（必须优先，先做再扩功能）

- [ ] 建立权限审批闭环（飞书交互卡片）
  - 范围：`write` / `exec` / `network` 必须人工审批，`read` 可按策略自动放行。
  - 要求：支持批准、拒绝、超时；审批结果可回传并驱动任务继续/终止。
  - 验收：未审批前不能执行高风险操作；审批后行为与决策一致。

- [ ] 建立最小测试基线（当前无测试）
  - 范围：命令解析、审批策略、会话重置/打断、审批回调流程。
  - 要求：`vp test` 可执行且有基础覆盖。
  - 验收：新增回归风险点必须有对应单元/集成测试。

## P1（高优先，支撑后续扩展）

- [ ] 重构 `feishu-gateway.ts`（当前约 884 行）
  - 目标：按职责拆分，降低耦合和改动风险。
  - 建议拆分：
    - 消息入口与去重队列
    - 指令路由/解析
    - 会话控制（启动、重置、打断、session 恢复）
    - 卡片渲染与增量更新
  - 验收：单文件显著瘦身，新增指令/审批无需改动多个不相关模块。

- [ ] 完善指令系统（`/xx`）
  - 第一批：
    - `/help`：展示支持指令
    - `/new`：保留现有行为
    - `/model`：展示当前模型/可用模型
    - `/model <name>`：切换到指定模型
  - 第二批（可选）：
    - `/status`、`/cwd`、`/stop` 统一纳入路由层
  - 验收：新增指令不需要改动核心消息处理主流程。

## P2（结构治理与清理）

- [ ] 调整 `chat-state-store.ts` 位置
  - 现状：位于 `feishu/` 目录，语义偏“会话状态持久化”，非飞书专属。
  - 建议：迁移到 `session/` 或 `state/` 目录，并抽象接口供多入口复用。
  - 验收：状态存储与 IM 渠道解耦。

- [ ] 删除无用代码与占位逻辑
  - 目标：清理未接线模块、无效调用、历史调试残留。
  - 验收：删除后 `vp check` 通过，行为不回退。

- [x] 评估并收敛 `server/` 目录
  - 目标：如果后续仅通过飞书调试，移除本地 debug http 入口。
  - 说明：已确认不支持云端桥接，`ws-client` 已移除。
  - 验收：启动路径清晰，入口最小化。

## 建议执行顺序

1. P0：审批闭环 + 最小测试基线
2. P1：网关拆分 + 指令系统
3. P2：目录治理 + 无用代码清理 + server 收敛

## 里程碑定义

- M1：高风险操作可控（审批闭环上线）
- M2：可扩展（网关拆分完成，指令可快速新增）
- M3：可维护（目录清晰、冗余代码移除、测试可回归）
